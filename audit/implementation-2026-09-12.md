# 体验修复交付记录 · 2026-09-12

对应 `review-2026-09-12.md` 的问题，本轮已实施以下修改，未更改原始样本或人工异议标注。

## 已完成

- 首屏以阅读为主：默认显示 5 条有异议或前提的回答，并展开第一条；诊断与数据说明收纳到页尾。
- 原句高亮、上下文展开、评论逐字引用、来源链接与复制评论入口。明确链接不会自动定位原站评论。
- 增加标题相关筛选：当前样本 36 条相关回答、24 条扩展阅读；全部 60 条仍可查看，统计分母不变。
- 个人条件仅检索经过引用校验的条件评论，展示完整主体与否定句，不再推断“适合你”。逐项显示条件覆盖，零覆盖明确提示。
- 区分“没有取得评论”与“取得评论但未见异议”；同时展示原站评论总数和本次取得数。
- 禁用没有样本及不适用的话题入口；错误不再泄露本地路径。
- 切换、失败、取消均使旧数据失效；迟到响应不能恢复旧结果。增加超时、取消、重试和返回可用样本。
- 排序与条件调整在本地复用数据；服务端同话题同模式的分析缓存 15 分钟，共享并发请求，失败不保留为成功缓存。
- 使用原生进度条兼容严格 CSP；改善字号、对比度、焦点提示与窄屏布局。

## 验证

- `npm run check`：通过。
- `npm test`：87/87 通过，包含真实否定句、多条件覆盖、话题可用性、旧响应失效、缓存复用及失败重试回归。
- Edge 桌面 1720×889：首屏可见高亮原句及读者反驳；截图 `06-fixed-start.png`。
- Edge 390×844：页面内容宽度与视口同为 390px，无横向溢出；截图 `07-fixed-mobile.png`。
- 正常页面浏览器错误日志为空。
- 本地故障注入：先取得第一份工作结果，再切换考研话题模拟上游失败。错误页无旧列表、旧计数或旧筛选；点击“返回可用样本”恢复第一份工作离线数据。此项没有调用真实知乎或模型。
- 默认本地预览服务已重启至本轮代码，地址 `http://127.0.0.1:4318`。

## 仍然存在的产品边界

- 仅第一份工作话题有可用离线样本；考研样本仍待准备，信息型话题仍不适用。
- 实时凭据尚未配置，本轮没有完成真实知乎检索与模型分类端到端验收。
- 引文来源校验不能证明异议的语义分类正确，更不能证明异议本身成立。
- 每条最多 3 条精选评论，样本覆盖有限；不能将单次样本比例推定为产品最大覆盖率。
- 条件原话检索和标题相关筛选采用保守规则，可能漏选，不是个性化决策模型。

结论：本轮审计指出的主要阅读体验与状态管理问题已修复；可作为离线阅读演示使用，尚不能宣称实时多话题产品已完整验收。
